[project]
name = "sol-validator-prototype"
version = "0.1.0"
description = "Prototype validator and pathology corpus for RFC-SOL-0001 v0.1.0-draft"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["solval*"]
