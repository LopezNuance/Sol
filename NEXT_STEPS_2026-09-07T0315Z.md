# Sol — Next Steps Toward Formal Submission

Generated: 2026-09-07 03:15Z (UTC)
Scope: RFC-SOL-0001 (`Sol_RFC.md`) and companion RFC-SOL-QRY-0001 (`documentation/`)

## Status (updated 2026-09-08 00:50Z)

- **Phase 0: CLOSED.** Venue decided: internal (now) -> public spec (next) ->
  IETF-style (final). Decision note:
  `docs/decisions/2026-09-07-phase0-venue.md`. Versioning policy: the first
  unfreeze promotes v0.1.0-draft to v0.1.0 and carries the batched unfreeze
  candidates.
- **Phase 1: CLOSED (2026-09-07).** All 11 v0.1-blocking open questions
  resolved with dated decision notes:
  `docs/decisions/2026-09-07-v01-blocking-questions.md`. Q1 was resolved by
  implementation: `container_benchmark/` (harness + `report.json`) measured
  REQ-014.1..014.5 for exploded-dir / zip-flat / tar over the real GOLD-01
  artifact and a 16 MiB synthetic variant. Decision: single-file ZIP, STORED
  entries, layout identical to `artifact.sol.d/`.
  9 of 11 decisions are unfreeze candidates batched into the v0.1.0
  promotion; 2 are errata-class.
- **Phase 2: CLOSED (2026-09-07).** All nine Appendix C schema classes
  published (v0.2 set; `object.schema.json` added) and enforced by the
  validator via JSON Schema draft 2020-12. New stable rule IDs V0-07..V0-13
  (unfreeze candidates, batched into v0.1.0). Corpus extended to 37 cases
  (PATH-026..PATH-031 exercise the new rules); 37/37 pass with exact
  diagnostic match under `--strict-extra`. Three pre-existing corpus
  expected-list gaps (PATH-003/007/008) were corrected to match the
  validator's legitimate output (corpus-entry bucket, no spec change).
  Docs: `sol_validator/schemas/README.md`.
- **Phase 3: CLOSED (2026-09-07).** Both recorded errata applied and logged
  in `docs/errata/RFC-SOL-0001-errata.md`. E-002 (V0-04 scope vs
  `context_status`) landed as a clarifying sentence in Appendix A plus a
  Status-section pointer; E-001 (duplicate `9999...` example digest) was
  resolved by construction during GOLD-01 assembly (Phase 4). Both batch
  into the v0.1.0 promotion.
- **Phase 4: CLOSED (2026-09-07).** MVP implementation surface complete:
  `.solnb` container (`solval/container.py`, Q1 ZIP/STORED; validator
  accepts `.solnb` directly), GOLD-01 reassembled from the spec examples
  (sections 15/16/18/21-27/30/41/42/47/48; real digests; zero diagnostics;
  absorbs E-001), safe HTML/Markdown renderer (`solval/render.py`),
  reference executor (`solval/executor.py`, linear + partial-success +
  failure records), `.ipynb` import with Q6 classification
  (`solval/import_ipynb.py`). All 25 acceptance criteria demonstrated
  (`docs/acceptance/2026-09-07-acceptance-criteria.md`); 37/37 corpus green
  under `--strict-extra`; 12/12 demonstration checks
  (`sol_validator/demos/manifest.json`).
- **Phase 5: CLOSED (2026-09-08).** v0.1.0-draft unfrozen and promoted to
  **v0.1.0**: errata log (E-001, E-002) + nine batched unfreeze candidates
  (Q1, Q2, Q3, Q5, Q6, Q7, Q8, Q9, Q11) + two errata-class clarifications
  (Q4, Q10) + V0-07..V0-13 rule catalog entries landed in `Sol_RFC.md`
  (Status, Change Control, §14.6, §15, §16.4, §18, §20, §23, §24, §41,
  §42, §43, §48, §53, §59, Appendix A, Appendix C). docx export
  regenerated from the promoted markdown. Internal review (venue stage 1)
  completed with all findings triaged: 8 stale Appendix B `Expected:`
  lines + 6 missing corpus entries (errata, appendix text), E-003 (§42
  example codec hygiene), promotion-date correction; **no unfreeze
  candidates raised**. Submission package frozen:
  `docs/submission/2026-09-08-v0.1.0-submission-package.md`. Venue stage 2
  (public spec: namespace/trademark review, IANA registration, public
  comment) and stage 3 (IETF-style) remain per the Phase 0 decision.
- **Venue stage 2 (public spec): IN PROGRESS (2026-09-12).** Namespace,
  trademark, and ecosystem review complete with one open action
  (trademark search, project owner):
  `docs/reviews/2026-09-12-stage2-namespace-review.md`. IANA media-type
  registration request drafted for `application/vnd.sol.notebook`
  (namespace verified available 2026-09-12); submission lands with or just
  after public release:
  `docs/registration/2026-09-12-iana-media-type-request.md`. Public
  comment cycle process defined (channel and window pending owner
  confirmation): `docs/process/2026-09-12-public-comment-cycle.md`.
  Project Name section updated to record the review outcome (E-004); docx
  export regenerated. Public repository sync deferred until the local work
  completes (owner direction 2026-09-12).

## 1. Current state (verified 2026-09-07)

| Item | State | Evidence |
|---|---|---|
| RFC-SOL-0001 | **v0.1.0** (promoted 2026-09-08 from the v0.1.0-draft freeze; submission package frozen for the internal venue) | `Sol_RFC.md` Status section |
| RFC-SOL-0001 content | 63 sections: 10 invariants, V0-V5 validation catalog, MVP scope (39 items, §57), acceptance criteria (25 items, §58) | `Sol_RFC.md` |
| Open questions | 11 v0.1-blocking **resolved** (2026-09-07, landed in v0.1.0); 12 post-v0.1 remain | `Sol_RFC.md` §59 |
| Errata | 3 entries applied and logged (E-003 added in the Phase 5 internal review, 2026-09-08) | `docs/errata/RFC-SOL-0001-errata.md` |
| Reference validator | Implemented, V0-V5 as executable checks; 37-case corpus (GOLD-01..06, PATH-001..031) — **37/37 pass** under `--strict-extra` (re-run 2026-09-08) | `sol_validator/`, `python -m solval.validate_corpus corpus --strict-extra` |
| Validator gaps | Closed (2026-09-07): `.solnb` container, executor, safe renderer, `.ipynb` import implemented; JSON Schema enforcement live (V0-07..V0-13) | `sol_validator/README.md` |
| Published schemas | All 9 Appendix C schema classes published (v0.2 set; object records complete the set) and enforced by the validator | `sol_validator/schemas/README.md` |
| docx export | Regenerated 2026-09-08 from the promoted markdown (pandoc 3.7); probes: "Final Freeze Review" present, "v0.1.0-draft, frozen" absent | `documentation/RFC-SOL-0001-...docx` |
| RFC-SOL-QRY-0001 | v0.3.0 **FROZEN (2026-09-11)**; freeze gate **19/19 passed, status FROZEN** | `documentation/RFC-SOL-QRY-0001-v0.3.0.md` §44; `freeze_report_v030.json` (historical `freeze_report.json` retained) |
| QRY gate failure | Condition 8: "Exact evaluator runs over the authoritative exploded RFC-SOL-0001 representation" — blocker: no authoritative exploded representation profile supplied | `freeze_report.json` condition 8 |
| QRY evidence reproducibility | **Resolved (2026-09-11):** the v0.3.0 freeze report (`freeze_report_v030.json`) cites only committed repo artifacts and every digest recomputes via the gate harness. The historical `freeze_report.json` (v0.1 reconstruction) cited out-of-repo paths; it is retained as historical evidence only | `freeze_report_v030.json`; `qry_reference_impl/qryref/gate_check.py` |
| QRY submission package | Frozen for public-spec submission (stage 2), alongside the base RFC | `docs/submission/2026-09-11-qry-v0.3.0-submission-package.md` |
| Naming | "Sol", `.solnb`, `application/vnd.sol.notebook` — namespace and ecosystem review complete 2026-09-12; trademark search is a required open action (project owner) | `docs/reviews/2026-09-12-stage2-namespace-review.md`; `Sol_RFC.md` Project Name section |

Bottom line: the base RFC is frozen and its validator corpus is green, but the freeze was for *implementation*, not submission. The path to a formal submission runs through implementation: the freeze review itself states the remaining open questions "are ones that implementation answers better than analysis."

## 2. Gap analysis: what formal submission requires

1. **A stable, non-draft version.** "v0.1.0-draft, frozen" cannot be submitted as-is; the Change Control section requires unfreeze + version bump for any normative change, and the 11 blocking open questions are normative-relevant.
2. **Published normative artifacts.** Appendix C requires published schemas for 10 artifact classes; §59 Q11 requires the machine summary schema as a normative artifact. Only schema-light drafts exist.
3. **A physical container.** The MVP (§57 item 1) requires the `.solnb` container; only the non-normative exploded debug representation exists.
4. **Validating examples.** Change Control gate: "Complete examples intended for conformance use must validate." GOLD-01 has not been assembled from the spec examples, and the freeze review flagged a duplicate example digest that breaks that gate.
5. **MVP surface beyond the validator.** Executor, safe HTML/Markdown rendering, and `.ipynb` import are in MVP scope but unimplemented.
6. **Acceptance demonstration.** The 25 acceptance criteria (§58) need a recorded demonstration, not just a passing pathology corpus.
7. **Namespace clearance.** Name, extension, and media type are explicitly pending review; a media type of the form `application/vnd.sol.notebook` implies an IANA registration if this is a public standard.
8. **External review.** A formal submission needs a review/comment cycle on the promoted version, with findings triaged (errata / corpus entry / unfreeze candidate) per the freeze review's discipline.
9. **Reproducible companion evidence.** The QRY freeze gate evidence is not reproducible from this repo (see state table).

## 3. Next steps

### Phase 0 — Decide the submission venue (decision, ~1 session) — CLOSED 2026-09-07

Decision: internal venue now, public spec next, IETF-style final.
See `docs/decisions/2026-09-07-phase0-venue.md`.

- Determine what "formal submission" means: IETF-style RFC, an internal standards process, or public specification publication.
- Record the venue's requirements (versioning policy, review process, schema publication channel, media-type registration, trademark review) as a checklist in the repo.
- This gates the target version number and the depth of the external review in Phase 5.

### Phase 1 — Resolve the 11 v0.1-blocking open questions by implementation (§59) — CLOSED 2026-09-07

All 11 resolved; decisions and rationale in
`docs/decisions/2026-09-07-v01-blocking-questions.md`. Q1 evidence in
`container_benchmark/report.json` (tar disqualified at ~21x read amplification
for bounded reads; ZIP chosen; exploded dir retained as non-normative debug
format). Original tasking below, kept for the record:

The freeze review assigned each question a resolution path; execute them:

1. **Physical container (Q1):** benchmark candidate containers (e.g., ZIP-based, tar-based, OCI layout) against REQ-014.1–REQ-014.5 (bounded manifest read, structural index, random object access, integrity, progressive loading); record the decision and rationale.
2. **Minimum environment manifest (Q2):** define the minimum set of environment fields a run record must carry for provenance (acceptance criterion 11).
3. **Mandatory compression codecs (Q3):** fix the codec list (MVP item 30 requires lossless compression).
4. **Branch support minimum (Q4):** fix the v0.1 branch floor (MVP item 8 requires at least one branch).
5. **Minimum object-size support (Q5):** set the minimum object size a conforming store must handle.
6. **Imported `.ipynb` output classification (Q6):** define how imported outputs are classified (acceptance criterion 15).
7. **Cell-summary strictness (Q7):** resolve via the published cell schema (Phase 2).
8. **Mandatory actor fields for agent-authored commits (Q8):** resolve via the published actor schema (Phase 2).
9. **Minimal claim/evidence schema (Q9):** resolve via the published record schemas (Phase 2).
10. **Mandatory semantic-record status fields (Q10):** resolve against the status model (§20) and record schemas (Phase 2).
11. **Machine summary schema (Q11):** publish `machine_summary.schema.json` as a normative artifact (Phase 2).

Each resolved question gets a dated decision note in the repo (errata if wording-only, version bump if normative).

### Phase 2 — Publish the normative JSON Schemas — CLOSED 2026-09-07

All items below were encoded and enforced; see `sol_validator/schemas/README.md`
for the published set and `docs/decisions/2026-09-07-v01-blocking-questions.md`
(execution update) for the V0-07..V0-13 rule catalog addition. Original
handoff, kept for the record:

- Q2 -> `manifest.schema.json`: `environment` block with `environment_id`,
  `package_manifest`, `kernel`, `kernel_version` required; `sbom_ref` SHOULD.
- Q3 -> `object` records: `compression` restricted to the registered
  identifiers; conformance floor none+gzip.
- Q5 -> object store: size bound 0 B .. 1 GiB (validator diagnostic for
  out-of-range `put`).
- Q6 -> import metadata + status values for imported outputs
  (`staleness: unknown`, `verification: unverified`).
- Q7 -> `cell.schema.json`: `summary` required, non-empty, <= 500 chars.
- Q8 -> `actor.schema.json`: agent actors require `model_id`,
  `model_version`, `delegated_by`, `autonomy_level`.
- Q9 -> `record.schema.json`: minimal claim/evidence field sets.
- Q10 -> `record.schema.json`: all three status dimensions required on all
  record types; vacuous dimensions default, never omitted.
- Q11 -> `machine_summary.schema.json`: add `failures`, `diagnostics`,
  `open_proposals`, `stale`, `renders` to the current draft.
- Missing schema class: **object records** (9th class per Appendix C).


- Upgrade the 9 schema-light files in `sol_validator/schemas/` into the published schemas Appendix C requires: manifest, cells, records, runs, commits, **object records (missing)**, actor records, machine summary, diagnostics.
- Enforce them in the validator (replace "basic structural checks" with real JSON Schema validation) and keep diagnostics deterministic per Appendix C.
- Re-run the 31-case corpus after enforcement; add schema-validation corpus cases where enforcement surfaces new diagnostics.

### Phase 3 — Apply the two recorded errata — CLOSED 2026-09-07

1. **Duplicate example digest:** §18 `contract_hash` and §22 `instruction_ref` share digest `9999...` for different content. Fix during GOLD-01 assembly (Phase 4) so the conformance examples validate per the Change Control gate.
2. **V0-04 scope vs `context_status`:** add the clarifying sentence that V0-04 applies to `lifecycle`/`staleness`/`verification` statuses while `context_status` remains open pending the post-v0.1 registry decision.

Both are errata-class; batch them into the Phase 5 version bump with an errata log entry.

### Phase 4 — Complete the MVP implementation surface — CLOSED 2026-09-07

- Implement the physical `.solnb` container per the Phase 1 decision; keep the exploded debug representation as the corpus format.
- Assemble **GOLD-01 from the spec examples** and make it validate (this is the Change Control "examples must validate" gate; it also absorbs erratum 1).
- Implement the remaining MVP items: executor (linear execution, partial-success runs, failure records), safe HTML/Markdown rendering from selected commits without code execution, and `.ipynb` import with provenance warnings.
- Record a demonstration of all 25 acceptance criteria (§58) — a checklist with links to the artifacts/runs that satisfy each.

### Phase 5 — Version promotion and submission package — CLOSED 2026-09-08

Done (internal venue stage):

- Unfroze per Change Control and promoted to **v0.1.0**, carrying the errata
  log, the nine batched unfreeze candidates, the two errata-class
  clarifications, and the V0-07..V0-13 rule catalog entries (all normative
  text verified against the published schemas before landing).
- Regenerated the docx export from the promoted markdown.
- Internal review (venue stage 1) completed; every finding triaged (errata /
  corpus entry / unfreeze candidate); no unfreeze candidates raised:
  `docs/reviews/2026-09-08-v0.1.0-internal-review.md`.
- Submission package frozen:
  `docs/submission/2026-09-08-v0.1.0-submission-package.md`.

Remaining at later venue stages (per the Phase 0 decision, not part of the
internal stage): namespace/trademark/ecosystem review + IANA media-type
registration + public comment cycle (stage 2); IETF-style conversion
(stage 3).

### Companion track — RFC-SOL-QRY-0001 (unblocked 2026-09-08)

The companion spec is one gate condition from freeze. Unblock progress:

1. **DONE (2026-09-08).** Authoritative RFC-SOL-0001 exploded representation
   profile published and designated: `solqry-authoritative-exploded/v1`
   (`docs/profiles/exploded-representation-profile.{md,json}`). Clears the
   condition 8 blocker at the designation level. Anchored to the promoted
   v0.1.0 baseline; instances are `corpus/` (31-case prototype lineage, moved
   in from the OpenAI sandbox) and `sol_validator/corpus/` (37-case current).
   Finding recorded: §56 example drift (errata-class).
2. **PARTIALLY CLOSED (2026-09-08).** A QRY reference implementation was moved
   into the repo at `qry_reference_impl/` (bound-algebra engine + validators,
   three JSON Schemas, Substrait profile, 69-case corpus `QRY-001..069`, proof
   certificates). It is a **reconstruction** (v0.1), explicitly not the
   original OpenAI sandbox package, and runs 69/69. It supplies the missing
   artifacts but does NOT reproduce the 18/19 (different design/digests) and
   does NOT satisfy the v0.3.0 §44 gate (no `sol:rewrite/*` /
   `sol:bound/avg_optional_prefix` rules, no `QGOLD-*`/`QPATH-*` cases).
   Gap + path-forward options: `docs/qry/2026-09-08-reproducibility-gap.md`.
3. **DONE (2026-09-08).** All 20 v0.3-blocking open questions (§43.1)
   resolved with dated decision notes:
   `docs/decisions/2026-09-08-qry-v03-blocking-questions.md`.
4. **DONE (2026-09-11, Option 3).** Extended the reconstruction toward
   v0.3.0 and re-ran the freeze gate to 19/19 with a fresh, honest run from
   this repo; **v0.3.0 is FROZEN**. Phased plan:
   `docs/qry/2026-09-08-v03-implementation-plan.md` (all 9 phases COMPLETE
   2026-09-08..2026-09-11 — answer-bound algebra, typed AST and
   canonicalization, exact evaluator, rewrite registry and proof kernel,
   normative corpus restructure, materialized and bounded semantic
   evaluators, machine-summary substitution, logical disclosure and
   leakage separation, gate re-run and freeze; corpus 136/136, 64/64
   normative green, 190 unit tests green). The gate harness
   (`qry_reference_impl/gate_check.py`) re-evidenced all 19 §44 conditions
   from committed artifacts with fresh digests; the freeze report is
   `freeze_report_v030.json` (19/19, status FROZEN, report digest
   `sha256:1ea510ee17a5d8b48677678c106f55a5a768b0b76f822bb771187c49c2884b83`),
   and the historical `freeze_report.json` (v0.1 reconstruction, 18/19,
   NOT_FROZEN) is retained untouched. The spec's status section records the
   freeze.
5. **DONE (2026-09-08).** Submission relationship decided: separate RFC,
   published as a companion specification to RFC-SOL-0001 (cross-referenced,
   independently versioned, submitted alongside the base RFC at the
   public-spec stage): `docs/decisions/2026-09-08-qry-submission-relationship.md`.

## 4. Dependencies and sequencing

```text
Phase 0 (venue)  ────────────────────────────────┐
Phase 1 (Q1-Q11, container) ─┬─► Phase 4 (MVP) ──┤
Phase 2 (schemas, Q7-Q11)  ──┘                   ├─► Phase 5 (promote, review, submit)
Phase 3 (errata) ────────────────────────────────┘
Phase 1 (representation decision) ─► QRY step 1 (profile) ─► QRY steps 2-4 (freeze v0.3.0)
```

- Phases 1 and 2 start immediately; they are independent of the venue decision and unblock everything downstream.
- Phase 4 depends on the container decision (Phase 1 Q1) and the published schemas (Phase 2).
- The QRY profile publication depends only on the base RFC's representation decision, so it can land before the full MVP.
- Phase 5 is the only phase that requires the venue decision (Phase 0).

## 5. Definition of done (submission-ready)

1. All 11 v0.1-blocking open questions resolved with dated decision notes.
2. All 10 Appendix C schema classes published and enforced by the validator; corpus green under enforcement.
3. Both recorded errata applied and logged.
4. Physical `.solnb` container implemented; GOLD-01 assembled from spec examples and validating.
5. All 25 acceptance criteria demonstrated with recorded evidence.
6. Promoted non-draft version with errata log; docx export regenerated from it.
7. Name/extension/media-type review complete (registration started if public).
8. External review cycle completed with findings triaged; no open unfreeze candidates blocking the submission version.
9. (Companion) QRY freeze gate 19/19 with all evidence reproducible from this repository.
